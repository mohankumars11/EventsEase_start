# Build output. Regenerated by `npm run build` on every deploy.
dist/

# esbuild, copied in rather than installed. See README.
node_modules/

# Verification screenshots. Useful locally, noise in a diff.
shots/
