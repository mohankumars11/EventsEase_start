<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ══════════════════════════════════════════════════════════════
         WHAT THIS APP IS ALLOWED TO SEE
         ══════════════════════════════════════════════════════════════

         Since Android 11 an app cannot enumerate other installed apps
         unless it declares what it is looking for. Without this,
         Razorpay checkout asks the system "is there a UPI app here",
         gets an empty answer, and does not offer UPI at all — which is
         why the sheet showed only cards, netbanking and wallets in the
         APK while the identical checkout in Chrome showed UPI first and
         took a real rupee.

         Declared by INTENT rather than by package name on purpose: a
         list of packages goes stale the day a new UPI app matters, and
         picking which wallets a customer may use is not a decision this
         manifest should be making. Anything that can handle a upi: link
         qualifies.

         Capacitor already hands the link over: Bridge.launchIntent fires
         an ACTION_VIEW intent for any scheme that is not the app own.
         It just could not RESOLVE one without this. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
    </queries>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 13 (API 33) made notifications an explicit runtime
         permission. targetSdk here is 36, so without this the OS silently
         drops every notification and the app has no way to ask.

         A master would install the app, be approved, receive offers the
         server thinks were delivered, and see none of them — which is the
         exact failure the whole Capacitor migration exists to fix, arriving
         through a different door. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Lets a job alert wake the screen. A 45-second offer that arrives
         silently in a pocket is an offer that expires. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
